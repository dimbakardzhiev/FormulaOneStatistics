<%= form_with(model: circuit, local: true) do |form| %>
  <% if circuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(circuit.errors.count, "error") %> prohibited this circuit from being saved:</h2>

      <ul>
      <% circuit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :circuit_name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :circuit_location %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country, id: :circuit_country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
